//__dirname gives us the directory in which the current program is running.
//express.static(__dirname + '/public') will make the public folder as a static website.
//so on the index.html file in public folder will be seen a static website. 
//$.post() does AJAX.